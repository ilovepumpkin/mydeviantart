<view>
    <view>
        <view>当前:</view>
        <picker-view indicator-style="height: 50px;" style="width: 100%; height: 300px;" value="{{value}}" bindchange="bindPickerChange">
            <picker-view-column>
                <view wx:for="{{authors}}" style="line-height: 50px">{{item.userId}}</view>
            </picker-view-column>
        </picker-view>
    </view>
</view>
